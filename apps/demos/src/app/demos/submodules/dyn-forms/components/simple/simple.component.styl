.mat-card
  margin-bottom: 0.5em

.mat-radio-group
  display: block
  // equalize to mat-form-field(outline)
  padding: 1.40em 0 0.74625em

.array-item
  position: relative

  // let the bootstrap row to use 100%
  button
    position: absolute
    right: 0
    top: 8px

.form-actions
  margin-top: 1em
